kernel-headers-system-docker:
  image: rancher/os-headers:v0.4.0-rc10
  labels:
    io.rancher.os.detach: false
    io.rancher.os.after: network
    io.rancher.os.scope: system
  volumes:
  - /usr/src:/usr/src
  - /lib/modules:/lib/modules
