kernel-headers:
  image: rancher/os-headers:v0.5.0-1
  labels:
    io.rancher.os.detach: false
    io.rancher.os.after: network
  environment:
  - KERNEL_HEADERS_URL
  volumes:
  - /usr/src:/usr/src
  - /lib/modules:/lib/modules
