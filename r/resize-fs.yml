resizefs:
  image: rancher/os-resizefs:v0.4.3-rc4
  environment:
  - RESIZE_DEV
  privileged: true
  labels:
    io.rancher.os.detach: false
    io.rancher.os.scope: system
    io.rancher.os.before: console
  volumes:
  - /var/log:/var/log
